# AI駆動開発テンプレート運用ガイド

このリポジトリは、アイデアから実装までを一貫して進めるためのテンプレートです。記録は必要な情報だけに絞り、同じ内容を複数のファイルへ転記しません。

## 作業前に読むもの

`docs/` 全体を無条件に読まないでください。作業に関係するものだけを、次の順に確認します。

1. `AGENTS.md`
2. 関連する `docs/00_base/` の内容
3. 継続中の作業がある場合は、関連する `docs/03_task/` の記録
4. 関連する `docs/01_decision/` の記録
5. 必要な場合だけ `docs/02_open/` の未解決事項

`docs/01_decision/`、`docs/02_open/`、`docs/03_task/` に複数のファイルがある場合は、ファイル名や検索で関連するものを選びます。全ファイルを毎回読む必要はありません。

### 補足

- 実装中に前提やスコープが変わり、現在の実装方針やタスクでは適切に進められなくなった場合は、実装を続ける前に関連する `docs/00_base/` と `docs/03_task/` を更新する。
- 複数ステップの作業や後続セッションへ持ち越す作業は `docs/03_task/` で管理し、作業を中断・終了する前に現在の進捗と次に進める箇所が分かる状態へ更新する。
- レビュー指摘は、その場で修正できる場合は修正して終える。対応内容が明確で後へ持ち越す場合は `docs/03_task/`、対応方針が未確定の場合は `docs/02_open/`、長期的な判断につながる場合は `docs/01_decision/` に記録する。

## docs ディレクトリの役割

- `docs/00_base/00_idea.md`: 目的、想定ユーザ、MVP、非目標、成功条件を整理する。
- `docs/00_base/01_implementation.md`: 技術、設計、実装スコープ、テストや品質確認の方針を整理する。
- `docs/01_decision/`: 後続の作業や別セッションでも必要になる、長期的に有効な判断と理由だけを残す。記録を作成・整理する場合は `00_template.md` を参照する。
- `docs/02_open/`: 現在は解決できず、対応内容や方針が未確定の事項だけを残す。解決後は削除する。記録を作成・整理する場合は `00_template.md` を参照する。
- `docs/03_task/`: 現在進行中または後続へ持ち越す作業と、その現在の進捗を管理する。AIエージェントは可逆的なリポジトリ内編集、既存の検証、task更新を自律的に進めてよい。詳細な選択、検証、停止の運用は `00_template.md` を参照する。

### `00_base` のライフサイクル

- `docs/00_base/` は現在進行中の1開発サイクルの基準となる情報だけを表し、日々の進捗は記録しない。
- すべてのタスク完了後に `00_idea.md` のMVPと成功条件を満たしていることを確認する。
- 開発サイクル完了時に、将来必要な判断を decision、未解決事項を open へ残し、次の作業開始時に2ファイルを再利用する。
- 過去の内容が必要な場合は Git 履歴を参照する。

## 記録する条件

新しい Markdown を残すこと自体を目的にしません。その場で完結する調査、修正、レビュー結果は、原則として記録しません。

decision / open / task の作成・整理ルールは、それぞれの `00_template.md` に従います。

## 作業上の制約

- ユーザの依頼や既存の手順に含まれていない、不可逆な変更や外部環境への変更は勝手に実行しない。
- データ削除、本番環境への反映、force push、認証情報やsecretの変更など、影響の大きい操作が必要な場合は事前に確認する。
