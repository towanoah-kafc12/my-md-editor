# File Operation Tasks

サイドバー、OSファイル操作、画像ファイルなど、ローカルファイルを直接扱う機能をまとめる。

ファイル操作系のUIは個別に増やさず、先に共通の右クリックメニューを整備し、そこへ順次追加する。

## ファイル / フォルダ共通の右クリックメニュー

Status: 未着手

Priority: 高

Dependencies: なし

Success criteria: 該当なし

### Work

- [ ] 実装前にこのタスクの内容を確認し、未確定事項 / 実装範囲 / 受入条件を必要な粒度まで詰める
- [ ] ワークスペース / フォルダ / Markdownファイルを共通の右クリック対象として扱える状態モデルを作る
- [ ] 右クリックした対象と画面座標を保持し、対象に応じたメニューを表示できるようにする
- [ ] メニュー外クリック / Escape / 項目選択後に確実に閉じるようにする
- [ ] 画面端でメニューが見切れない位置補正を行う
- [ ] キーボード操作と最低限のアクセシビリティを維持する
- [ ] 現在のRemove Folderを共通メニューへ統合する
- [ ] 後続機能が項目定義を追加するだけで載せやすい構造にする

### Done

- ワークスペース / フォルダ / Markdownファイルの各対象で右クリックメニューが開く
- 後続のファイル操作が同じメニューへ追加できる

### Verification

### Progress

### Note

New Markdown / New Folder / Rename / Reveal in Explorer / Copy Path の先行タスクとする。

## サイドバーからのファイル / フォルダ作成

Status: 未着手

Priority: 高

Dependencies: ファイル / フォルダ共通の右クリックメニュー

Success criteria: 該当なし

### Work

- [ ] 実装前にこのタスクの内容を確認し、未確定事項 / 実装範囲 / 受入条件を必要な粒度まで詰める
- [ ] ワークスペース直下を右クリックした場合はワークスペース直下を作成先にする
- [ ] フォルダを右クリックした場合はそのフォルダ内を作成先にする
- [ ] Markdownファイルを右クリックした場合は親フォルダを作成先にする
- [ ] New Markdown / New Folderを右クリックメニューへ追加する
- [ ] 名前入力UIを用意し、Enterで確定 / Escapeでキャンセルできるようにする
- [ ] New Markdownでは拡張子未指定時に`.md`を補完し、Markdown以外の拡張子は作成対象にしない
- [ ] 空文字、パス区切り、Windowsで利用できない文字、末尾の空白 / ピリオド、予約名などを検証する
- [ ] 既存ファイル / フォルダとの名前衝突を検知し、既存項目を上書きしない
- [ ] 実ファイルシステム側の作成失敗も必ずエラーとして扱う
- [ ] New MarkdownはUTF-8の空ファイルとして作成し、作成後に自動で開いて選択する
- [ ] New Folderは作成後に親ノードを展開し、新しいフォルダがツリーへすぐ反映されるようにする
- [ ] 作成失敗時はツリーへ存在しない項目を残さない

### Done

- サイドバーの右クリックメニューからMarkdownファイルとフォルダを安全に作成できる
- 作成したMarkdownを追加操作なしで編集開始できる

### Verification

### Progress

### Note

削除操作はこのタスクに含めない。

## ファイル / フォルダ名の変更

Status: 未着手

Priority: 高

Dependencies: ファイル / フォルダ共通の右クリックメニュー

Success criteria: 該当なし

### Work

- [ ] 実装前にこのタスクの内容を確認し、未確定事項 / 実装範囲 / 受入条件を必要な粒度まで詰める
- [ ] RenameをMarkdownファイル / フォルダの右クリックメニューへ追加する
- [ ] 現在名を初期値にした名前入力UIを用意する
- [ ] ファイル作成と同じ名前検証を再利用する
- [ ] Markdownファイルは`.md`以外へ変更できないようにする
- [ ] 既存項目との衝突時に上書きせずエラーにする
- [ ] Rename後にツリー上の名前 / path / idなどパス由来の状態を整合させる
- [ ] 開いているMarkdownをRenameしても編集内容、未保存状態、選択状態を維持する
- [ ] フォルダRename時は配下のMarkdownパスを一括更新し、開いている文書との対応を維持する
- [ ] OS側のRename失敗時はアプリ内状態を変更しない
- [ ] 画像貼り付け機能実装後は`<stem>.assets/`とMarkdownファイルのRename連動方針を最初の内容詰めで再確認する

### Done

- サイドバーからファイルとフォルダを安全にRenameできる
- Rename後も開いている文書をそのまま編集 / 保存できる

### Verification

### Progress

### Note

削除機能は事故防止のため別タスクとして必要性を判断する。

## Explorerで表示 / パスをコピー

Status: 未着手

Priority: 中

Dependencies: ファイル / フォルダ共通の右クリックメニュー

Success criteria: 該当なし

### Work

- [ ] 実装前にこのタスクの内容を確認し、未確定事項 / 実装範囲 / 受入条件を必要な粒度まで詰める
- [ ] Reveal in Explorerをワークスペース / フォルダ / Markdownファイルの右クリックメニューへ追加する
- [ ] MarkdownファイルではExplorerを開いたうえで対象ファイルを選択状態にできる方法を優先する
- [ ] Copy Pathを同じ右クリックメニューへ追加する
- [ ] Copy PathはOS上の絶対パスをクリップボードへコピーする
- [ ] OS操作はTauri側へ閉じ込め、Svelte側へWindows固有コマンドを直接持ち込まない
- [ ] macOS対応時にFinder等へ差し替えられる境界を保つ
- [ ] OS操作失敗時は非侵襲的に通知する

### Done

- Plainmarkから対象のOS上の場所を開ける
- 対象の絶対パスをコピーできる

### Verification

### Progress

## ファイル / フォルダのドラッグ&ドロップ

Status: 未着手

Priority: 中

Dependencies: なし

Success criteria: 該当なし

### Work

- [ ] 実装前にこのタスクの内容を確認し、未確定事項 / 実装範囲 / 受入条件を必要な粒度まで詰める
- [ ] OSからPlainmarkへのファイル / フォルダdropイベントを受け取る
- [ ] `.md`ファイルをドロップした場合は文書として開く
- [ ] すでに同じMarkdownが開かれている場合は重複追加せず、その文書を選択する
- [ ] フォルダをドロップした場合は既存のフォルダ選択と同じWorkspaceとして開く
- [ ] 複数項目の同時ドロップを扱えるようにする
- [ ] Markdown以外の通常ファイルは無視し、必要なら対象外であることを通知する
- [ ] 画像ファイルをエディタ上へドロップした場合はMarkdownへの画像貼り付けタスク側へ処理を委譲できる構造にする
- [ ] ドロップ処理中の読込失敗で既存ワークスペースや文書状態を壊さない

### Done

- ファイルダイアログを使わず、Markdownファイルまたはフォルダをドロップして編集を開始できる

### Verification

### Progress

## Markdownへの画像貼り付けとローカル保存

Status: 未着手

Priority: 高

Dependencies: なし

Success criteria: 該当なし

### Work

- [ ] 実装前にこのタスクの内容を確認し、未確定事項 / 実装範囲 / 受入条件を必要な粒度まで詰める
- [ ] Crepeの現在のImageBlock無効化を確認し、ローカル画像表示に必要な最小構成だけを有効化する
- [ ] クリップボードに画像データがある場合の貼り付けを検知する
- [ ] クリップボード画像は初期実装ではPNGとして保存する
- [ ] エディタ上へ画像ファイルをドロップした場合も同じ保存 / 挿入処理を使う
- [ ] ファイルドロップの初期対応形式はPNG / JPEG / WebP / GIFを基本とし、SVGなど追加形式は最初の内容詰めで必要性を判断する
- [ ] 保存先はMarkdownと同じ階層の`<Markdownファイル名のstem>.assets/`とする
- [ ] assetsフォルダがなければ自動作成し、存在する場合はそのまま再利用する
- [ ] 画像名は時刻等を含む衝突しにくい規則にし、同名が存在する場合は連番等で確実に回避する
- [ ] ファイルドロップでは元画像を移動せずassetsフォルダへコピーする
- [ ] Markdownにはassetsフォルダへの相対パスを挿入し、Windowsでも区切り文字は`/`を使う
- [ ] 空白などを含むパスでもCrepe / CommonMarkで保存後に再読込できるリンク形式にする
- [ ] 初期alt textは空でよいものとし、通常のMarkdown編集で後から変更できるようにする
- [ ] 保存先が読み取り専用 / 書き込み不能の場合はMarkdownへ壊れた参照を挿入せずエラーを表示する
- [ ] 画像保存に成功した後だけMarkdown参照を挿入する
- [ ] UndoでMarkdown上の画像参照を削除しても、すでに保存した画像ファイルは自動削除しない
- [ ] 同じ画像を複数回貼り付けても既存ファイルを上書きしない
- [ ] 外部サービスへのアップロード処理を入れず、画像もローカルファイルだけで完結させる

### Done

- クリップボード画像を貼り付けるだけでローカル保存され、その場でMarkdown内に表示される
- 対応画像ファイルをドロップしても同じ形式で取り込める
- 保存後にアプリを再起動しても画像参照が維持される
- Undoや再編集で既存画像ファイルが意図せず削除されない

### Verification

### Progress
