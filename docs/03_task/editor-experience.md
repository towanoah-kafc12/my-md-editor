# Editor Experience Tasks

Markdownの移動、検索、表示設定など、編集そのものを軽く使いやすくする機能をまとめる。

## Ctrl+P ファイルクイックオープン

Status: 未着手

Priority: 高

Dependencies: なし

Success criteria: 該当なし

### Work

- [ ] 実装前にこのタスクの内容を確認し、未確定事項 / 実装範囲 / 受入条件を必要な粒度まで詰める
- [ ] Ctrl+P / Cmd+Pでファイル選択UIを開く
- [ ] 開いている全ワークスペース内のMarkdownを候補にする
- [ ] ファイル名を主対象にして部分一致で絞り込めるようにする
- [ ] 同名ファイルを区別できるよう、必要な場合だけ親フォルダ等の短いパスを表示する
- [ ] 上下キーで候補移動、Enterで選択、Escapeで閉じられるようにする
- [ ] 選択したMarkdownが未読込なら通常のファイル選択処理と同じ経路で読込む
- [ ] 選択後はサイドバー上の選択状態も同期する
- [ ] ワークスペースが空の場合でもUIが壊れないようにする
- [ ] 全文検索とは分離し、本文のインデックス作成をこのタスクへ持ち込まない

### Done

- ファイルツリーを辿らず、キーボードだけで目的のMarkdownを開ける

### Verification

### Progress

## Markdownリンクから別ファイルを開く

Status: 未着手

Priority: 高

Dependencies: なし

Success criteria: 該当なし

### Work

- [ ] 実装前にこのタスクの内容を確認し、未確定事項 / 実装範囲 / 受入条件を必要な粒度まで詰める
- [ ] Crepe上のリンク操作を捕捉し、通常編集とリンク移動を両立する操作方法を決める
- [ ] 相対パスで参照された`.md`リンクを現在のMarkdownファイル位置から解決する
- [ ] 解決先が開いているワークスペース内なら既存文書を選択する
- [ ] ワークスペース内だがまだ文書一覧へ載っていない場合でも、安全に読込んで選択できるようにする
- [ ] ワークスペース外のローカルMarkdownを開くかどうかを最初の内容詰めで決め、許可する場合はOther Files相当として扱う
- [ ] URLスキームがhttp / https等の外部リンクならOSの既定ブラウザで開く
- [ ] 存在しないローカルリンクでは新規ファイルを暗黙作成せず、見つからないことを通知する
- [ ] `#heading`など同一文書内アンカーの扱いを確認し、Crepe標準挙動を壊さない
- [ ] `[[Wiki Link]]`などMarkdown標準外のリンク記法は対象にしない

### Done

- 標準Markdownリンクを使ってローカルMarkdown間を移動できる
- 外部URLとローカルファイルを誤って同じ方法で処理しない

### Verification

### Progress

## フォントサイズ変更とショートカット

Status: 未着手

Priority: 中

Dependencies: なし

Success criteria: 該当なし

### Work

- [ ] 実装前にこのタスクの内容を確認し、未確定事項 / 実装範囲 / 受入条件を必要な粒度まで詰める
- [ ] 現在のEditor本文サイズを基準値として確認し、既定値を決める
- [ ] 初期実装の適用範囲はEditor内だけとし、サイドバー / 設定画面 / ステータスバーのサイズは変えない
- [ ] 設定画面へフォントサイズ項目を追加する
- [ ] 最小値 / 最大値 / 変更刻みを決め、極端な値を保存できないようにする
- [ ] 本文だけでなく見出し等も相対関係を保って読みやすく拡大 / 縮小できるCSS構造にする
- [ ] Ctrl/Cmd + `+`で1段階拡大する
- [ ] Ctrl/Cmd + `-`で1段階縮小する
- [ ] Ctrl/Cmd + `0`で既定値へ戻す
- [ ] WebView自体のズームが同時に発生しないようショートカットを処理する
- [ ] フォントサイズを既存のfont / theme設定と一緒に永続化する
- [ ] 旧設定にfontSizeが存在しない場合は既定値を補い、既存利用者の設定を壊さない
- [ ] 設定変更は再起動なしで現在のEditorへ即時反映する

### Done

- 設定画面と一般的なショートカットの両方からEditor文字サイズを変更できる
- 再起動後も設定値が維持される
- アプリUI全体が意図せず拡大 / 縮小されない

### Verification

### Progress

## 見出しアウトライン

Status: 未着手

Priority: 中

Dependencies: なし

Success criteria: 該当なし

### Work

- [ ] 実装前にこのタスクの内容を確認し、未確定事項 / 実装範囲 / 受入条件を必要な粒度まで詰める
- [ ] 現在のMarkdownからH1-H6を順序付きで取得する
- [ ] 見出し階層が分かる最低限のインデント表示を行う
- [ ] 常設の第2サイドバーは増やさず、必要時だけ開くポップオーバー / パネル等のUIにする
- [ ] アウトライン項目を選択すると対応する見出し位置へスクロールし、編集位置を移せるようにする
- [ ] Markdown編集に応じて見出し一覧を更新する
- [ ] 大きなMarkdownでも入力のたびに重い再解析を行わない更新方法を選ぶ
- [ ] 見出しがない文書では空状態を簡潔に表示する

### Done

- 長いMarkdown内を見出し単位ですばやく移動できる
- アウトラインを使わない通常編集時の画面領域を圧迫しない

### Verification

### Progress

## 全文検索

Status: 未着手

Priority: 低

Dependencies: Ctrl+P ファイルクイックオープン

Success criteria: 該当なし

### Work

- [ ] 実装前にこのタスクの内容を確認し、未確定事項 / 実装範囲 / 受入条件を必要な粒度まで詰める
- [ ] 開いているワークスペース内のMarkdown本文を検索対象にする
- [ ] ファイル名検索のCtrl+Pとは別操作として提供する
- [ ] 検索語、ファイル名、該当箇所の短い前後文を結果に表示する
- [ ] 結果を選択すると対象Markdownを開き、可能なら該当箇所へ移動する
- [ ] 大文字 / 小文字、単純部分一致など初期検索仕様を決める
- [ ] バイナリ / Markdown以外のファイルを探索しない
- [ ] 大きなワークスペースでも入力やファイル切替を阻害しない方法を選ぶ
- [ ] 初期実装では高度な正規表現、置換、検索インデックスの永続化を必須にしない

### Done

- 複数Markdownを横断して本文を検索し、結果から該当文書へ移動できる
- 全文検索を使っていないときに常時大きな処理負荷を発生させない

### Verification

### Progress

### Note

軽量性を崩さないことを優先し、Ctrl+Pより後に実装する。
