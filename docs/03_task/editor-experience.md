# エディタ体験の拡張

## フォント設定

Status: 未着手

Priority: 高

Dependencies: なし

Success criteria: 該当なし

### Work

- [ ] UI・編集本文・見出しを含めたフォント適用範囲と、選択状態の保存先を調査する。
- [ ] 見出しを含めてゴシック体を既定にし、アプリ内から選択・即時反映できる設定を実装する。
- [ ] 再起動後の設定復元、Markdown 内容を変えないこと、Crepe の編集操作を確認する。

### Done

- 既定表示が見出しを含めてゴシック体である。
- 利用者がアプリ内で選択したフォントが即時反映され、再起動後にも維持される。
- Markdown の内容・保存形式・編集操作に影響しない。

### Verification

- 未実施。

### Progress

未着手。次は設定の保存方法と Crepe / ProseMirror のフォント指定箇所を調査する。

## ワークスペースフォルダ

Status: 未着手

Priority: 高

Dependencies: Windows 上での既存ファイル I/O 受入確認

Success criteria: 該当なし

### Work

- [ ] Tauri のフォルダ選択、Markdown の再帰列挙、ディレクトリ読み取り権限と scope を調査する。
- [ ] 複数 root、重複パス、相対表示、空フォルダ、読み取りエラー、未保存文書を含むデータモデルを設計する。
- [ ] 個別ファイルを開く機能を残し、選択した複数フォルダをサイドバーのセクションとして表示する。
- [ ] フォルダ内の Markdown を開く・切り替える・保存する動作とテストを実装する。

### Done

- 個別ファイルを開く既存導線を維持したまま、複数フォルダを 1 ウィンドウのサイドバーに区切って表示できる。
- 各フォルダ内の Markdown を選択、編集、保存できる。
- 重複、空フォルダ、読み取り失敗、未保存文書で文書状態を失わない。

### Verification

- 未実施。

### Progress

未着手。現行 MVP のフォルダツリー非目標を拡張するため、実装開始時に base 文書と decision を更新する。

## カラーテーマとコード表示

Status: 未着手

Priority: 中

Dependencies: フォント設定

Success criteria: 該当なし

### Work

- [ ] CSS カスタムプロパティをテーマ境界として整理し、テーマ選択と保存方法を設計する。
- [ ] Crepe / Milkdown のコードブロック DOM、ハイライト方式、拡張による依存サイズを調査する。
- [ ] VS Code 系テーマを含む候補を、コントラスト、ライセンス、可読性、依存サイズで比較する。
- [ ] 本文、リストマーカー、選択状態、コードブロックとシンタックスハイライトを一貫して切り替えられるよう実装する。

### Done

- テーマをアプリ内で選択し、再起動後にも維持できる。
- 通常本文とコードブロックが、各テーマで十分なコントラストを満たす。
- 採用テーマとコードハイライト方式の理由を decision に記録する。

### Verification

- 未実施。

### Progress

未着手。VS Code 系テーマの具体的な採用可否は未決定で、`docs/02_open/integrations-and-themes.md` を参照する。
